plugins {
    id 'signal-sample-app'
    alias(libs.plugins.compose.compiler)
}

android {
    namespace 'org.signal.contactstest'

    defaultConfig {
        applicationId "org.signal.contactstest.ryan"
    }
}

dependencies {
    implementation project(':contacts')
}